version: '3'
services:
  example:
    container_name: example
    build:
      dockerfile: ./Dockerfile
    ports:
      - "9999:8888"
    env_file: .env.dev
    restart: unless-stopped
    command: ["./scripts/start.sh"]
